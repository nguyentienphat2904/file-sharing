
        def download_file():
            return


        # # UI for DOWNLOAD
        # dowload_frame = tk.Frame(self.main_frame)
        # dowload_frame.pack(10)

        # dowload_hash_info_entry = tk.Entry(dowload_frame, width=30)
        # dowload_hash_info_entry.pack(side=tk.LEFT, padx=5)

        # download_button = tk.Button(dowload_frame, text="Download File", command=lambda: download_file() ,width=15)
        # download_button.pack(side=tk.LEFT)